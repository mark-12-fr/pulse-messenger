/* ============================================================
   Pulse Messenger — frontend configuration

   👉 This is the ONLY file you edit when deploying.

   - Local development: leave backendUrl as "" (empty). The frontend will
     talk to the same server that served the page (http://localhost:5000).
   - Production: set backendUrl to your Render backend URL, for example:
        backendUrl: "https://pulse-messenger-api.onrender.com"
   ============================================================ */
window.PULSE_CONFIG = {
  backendUrl: ""
};
